
engine: # default engine declaration used when no `engine` declaration is found.
  merge:
  - include: ["**"]
  - include: ['README.md']
    chain:
    - type: ReplaceText
      substitutions:
        - text: INSERT-TEXT-HERE
          with: "#files.contentsOf('test.txt')"
